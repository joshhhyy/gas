<%= form_for(@group) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :avatar %><br>
    <%= f.text_field :avatar, :value => 'https://ga-core.s3.amazonaws.com/production/uploads/program/default_image/128/thumb_WDI.png' %>
  </div>
  
  <div class="field">
    <%= f.label :instructor %><br>
    <%= f.text_field :instructor %>
  </div>

  <div class="actions">
    <%= f.submit :class => "btn btn-success" %>
  </div>
<% end %>
