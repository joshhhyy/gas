<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @group.name %>
</p>

<% @users.each do |user| %>
       <%= user.name %>
       <%= user.email %>
       <%= link_to 'Show', [@group, user] %>  <br>
    <% end %>


<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
