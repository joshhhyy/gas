<%= form_for(@quote) do |f| %>
  <% if @quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% @quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quote %><br>
    <%= f.text_area :quote %>
  </div>
  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.select :group_id, Group.all.map {|group|[group.name, group.id]}, :required => true %>
  </div>
  <div class="actions">
    <%= f.submit :class =>"btn btn-primary"%>
  </div>
<% end %>
